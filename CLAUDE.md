# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

Self-hosted DMARC aggregate report analyzer (PHP 8.3, strict types; MariaDB 10.11+; Apache; PDO/prepared statements; no framework). Full requirements live in `docs/dmarc-report-analyzer-spec.md` — read it before touching any subsystem described below, since the details (not just the existence) of each feature matter.

**Status: feature-complete against the spec.** Ingestion (decompress → parse → store, both DMARC XML and TLS-RPT JSON — spec §12), enrichment (`bin/enrich.php`), the domain health check (`bin/healthcheck.php`), the R1–R12 recommendation engine (`bin/analyze.php`), daily alerting (`bin/alert.php`), auth/RBAC (invitations, passkeys, TOTP, sessions, admin user management), and the dashboard (domain list + per-domain drill-down, spec §7.1/§7.2, plus the full set of domain-management mutating actions: onboard, approve baseline policy, edit `target_policy`/`non_sending`, and deactivate/reactivate — spec §15.1) are built and unit-tested. Only §10.8 community-threat-reporting is unbuilt, gated on a T&C/GDPR review that hasn't happened — it ships disabled either way (see `config.sample.php`). Two features beyond the original spec are also built: a public landing page (`docs/feature-landing-page.md`) and an in-app "DMARC 101" contextual help system (`docs/feature-helpsystem.md`) — see their own subsections below. Treat this split as current fact, not aspiration — verify against the file before assuming something is implemented.

## Commands

```bash
composer install                                  # install deps
vendor/bin/phpunit                                 # run all tests
vendor/bin/phpunit --filter testMethodName          # run a single test
vendor/bin/phpunit tests/ReportParserTest.php        # run one test file
vendor/bin/phpstan analyse src bin --level=6         # static analysis (also: composer stan)
vendor/bin/php-cs-fixer fix --dry-run --diff         # lint check (also: composer lint)
vendor/bin/php-cs-fixer fix                          # apply lint fixes
php -S 127.0.0.1:8080 -t public                      # dev server
php bin/ingest.php                                   # one ingestion pass (IMAP poll → store)
php bin/migrate.php                                  # apply pending db/migrations/*.sql
php bin/migrate.php --status                         # list applied/pending without running anything
php bin/enrich.php                                   # rDNS/ASN/known-senders labelling pass
php bin/healthcheck.php [domain] [--trigger=scheduled] # DNS/network posture check
php bin/analyze.php [domain]                         # R1-R12 recommendation engine
php bin/alert.php [domain]                           # daily alerting digest email
composer audit                                       # check dependency advisories
```

`bin/healthcheck.php` shells out to `dig` for DS records and resolver-pinned DNSBL queries (PHP's `dns_get_record()` can do neither) — `dig`/`host` must be installed (Debian/Ubuntu: `dnsutils`).

VS Code tasks (`.vscode/tasks.json`) wrap the same commands, plus `migrate` (`php bin/migrate.php`). Xdebug is configured on port 9003 (`.vscode/launch.json`) with a config for `bin/ingest.php` — adjust `pathMappings` there when debugging against the VPS instead of locally.

Setup not covered above: `cp config/config.sample.php config/config.php` and fill in (gitignored, mode `0640`); create the `dmarc` DB and run `php bin/migrate.php` then optionally load `db/seed-domains.sql` (the 10 real domains).

## Architecture

**Ingestion pipeline** (`bin/ingest.php`, the only fully-wired flow): IMAP poll via `webklex/php-imap` (never native `ext/imap` — unmaintained, unbundled in PHP 8.4) → `Decompressor` → `ReportParser` → `ReportStore`. Successes get archived (compressed, filename from content hash — never from the attacker-controlled attachment filename) and the IMAP message moved to the "done" folder; failures move to a "failed" folder for manual inspection rather than being dropped. Re-running ingestion is idempotent — dedup is enforced at the DB layer via `reports.raw_file_hash` and the `(domain_id, reporter_org, report_id)` unique constraint, not in application logic.

**`Decompressor` (`src/Ingest/Decompressor.php`)**: sniffs magic bytes (never trusts filename/Content-Type) to pick gzip/zip/raw-XML/raw-JSON. Gzip is inflated through a bounded stream (`inflate_add` in chunks, checked after every chunk) so a decompression bomb is caught mid-stream rather than after full inflation. Zip entries are capped by count and per-entry size before/while reading. This class is the primary defense against hostile `rua` submissions — the `rua` address is public DNS, so anyone can mail it. `sniffFormat()` (public) tells DMARC XML from TLS-RPT JSON post-decompression — gzip's magic bytes say nothing about the inner format, so this can only run after inflation (or on an already-plain payload).

**`ReportParser` (`src/Ingest/ReportParser.php`)**: `DOMDocument` + `libxml_use_internal_errors`, never passes `LIBXML_NOENT`/`LIBXML_DTDLOAD` (would re-enable entity substitution), feature-detects `LIBXML_NO_XXE` for PHP 8.4+/libxml 2.13+, and rejects any DOCTYPE outright as defense-in-depth. RFC 7489 elements map to the schema below; per-`<record>` parse failures are caught, logged as warnings on the `ParsedReport`, and skipped rather than failing the whole report — vendor XML deviates from spec constantly. Domain attribution uses `policy_published/domain`, not `report_metadata`, because the latter isn't reliably which-domain-is-this.

**`ReportStore` (`src/Ingest/ReportStore.php`)**: persists a `ParsedReport` idempotently against the schema below.

**TLS-RPT ingestion** (`TlsRptParser`/`TlsRptStore`, spec §3.7/§12): shares `bin/ingest.php`'s IMAP poll, `SenderRateLimiter`, size caps, and archive-then-quarantine discipline — `Decompressor::sniffFormat()` picks the branch post-decompression. Unlike DMARC XML (always one domain per file), an RFC 8460 JSON file's `policies[]` array can name multiple domains, so `ParsedTlsRptReport` isn't flattened to one domain like `ParsedReport` is (`$policies` is `list<array>`, matching `ParsedReport::$records`'s own array-not-object convention), and `TlsRptStore::store()` resolves/skips unmanaged domains per policy entry, not per file — a file with at least one matched domain is still archived and stored even if it also names domains this instance doesn't manage, deliberately looser than DMARC's per-file unmanaged-domain quarantine (routine multi-domain reports from large providers would otherwise constantly hit the Failed folder). Persists into `tls_rpt_reports`/`tls_rpt_records` (`db/migrations/0004_tls_rpt.sql`) — separate tables from DMARC's per spec §3.7, since the schemas don't map cleanly onto each other. Never touches `domains.current_published_policy` or `ip_enrichment` — both are DMARC-specific side effects of `ReportStore::store()` that don't map onto TLS-RPT's `policy-type` concept. `TlsRptCheck` (§11.2) already checks the `_smtp._tls` DNS record's *presence*; this is the separate report-*ingestion* half. No dashboard/alerting/recommendation surface — the spec doesn't ask for one.

**Schema migrations** (`bin/migrate.php`, `db/migrations/`): the schema is versioned as numbered, immutable `.sql` files applied in order and tracked in a `schema_migrations` table — never edit a committed migration in place; a schema change ships as a new file. MySQL/MariaDB DDL isn't transactional (implicit commit per statement), so keep each migration to one focused change. `db/migrations/0001_baseline.sql` is the full schema as of the switch to this system.

**Data model** (`db/migrations/`): `domains` (with three distinct policy fields — `current_published_policy` auto-read from DNS, `approved_baseline_policy` requires explicit admin approval, `target_policy` the desired end state; these serve different purposes and must not be collapsed into one field) → `reports` → `report_records` (one per source IP, `source_ip` as `VARBINARY(16)` via `INET6_ATON` for v4/v6 uniformity — see `src/Support/Ip.php`) → `auth_results`. Separately: `ip_enrichment`, `known_senders` (manual allowlist), `recommendations` (rule-engine output), `health_checks`/`health_check_items`, and the auth tables (`users`, `webauthn_credentials`, `recovery_codes`, `invitations`, `password_resets`, `sessions`, `audit_log`).

**Config** (`src/Config.php`): dot-path lookup (`$config->get('imap.host')`) over the array returned by `config/config.php`, loaded once. `require()` throws if a key is missing/empty — use it for anything that must not silently default. `Database::connect()` is a lazy singleton PDO with `EMULATE_PREPARES` off (real server-side prepared statements, not client-side interpolation).

**Enrichment** (`src/Enrichment/`, `bin/enrich.php`, spec §6): decoupled cron pass over `ip_enrichment` rows `ReportStore::touchEnrichment()` seeds on ingest (`source_ip`/`last_seen` only). `EnrichmentService` is PDO-free and orchestrates `RdnsResolver`/`AsnLookup` (both interface+real-impl+null/fake, so it's unit-testable) into a label via precedence: `known_senders` CIDR match (`KnownSenderMatcher`, reuses `Ip::inCidr()`) → ASN heuristic (`EspHeuristic`, small conservative `config('enrichment.known_esp_asns')` map) → `unknown`. Degrades to `NullAsnLookup` when the license-gated GeoLite2 `.mmdb` file isn't present, rather than failing. `known_senders` itself is edited via `KnownSendersController` (`src/Http/Controllers/KnownSendersController.php`, spec §3.6/§10.5/§15.1 — Admin tier, no step-up, add/delete only) — a rule takes effect the next time `KnownSenderMatcher::fromDatabase()` is called (`bin/enrich.php`, `bin/analyze.php`), since this controller only writes the table. `Ip::isValidIpOrCidr()` validates the free-text input at write time, mirroring `inCidr()`'s own subnet/bits parsing.

**Retention** (`bin/retention.php`, spec §13/§14): off by default — `retention.reports_years`/`retention.ip_enrichment_years` are unset/`0` unless explicitly configured, since the retention window is a DPO/operator policy call the spec deliberately doesn't assert a default for. `report_records`/`auth_results` have no date column of their own (dates live on the parent `reports` row), so pruning deletes old `reports` rows and lets `ON DELETE CASCADE` take the rest; `ip_enrichment` is pruned independently on its own `last_seen` cutoff. Once a window is set, the config value alone authorizes deletion on every cron run (no extra confirmation flag — same unattended-cron posture as `bin/alert.php`/`bin/analyze.php`), with `--dry-run` (mirrors `bin/migrate.php`'s `--status` boolean-flag pattern) to preview counts first. Never touches archived raw XML/JSON under `archive/` — spec §13 keeps that indefinite regardless. A real deletion is audit-logged (`retention.pruned`, `actor_user_id = null` — the same null-actor shape `AuthController::login()`'s `login.failed` entries already use for non-interactive events); `--dry-run` and no-op runs write nothing.

**Health check** (`src/HealthCheck/`, `bin/healthcheck.php`, spec §11): point-in-time DNS/network posture probe, independent of report data — one `HealthCheck`-interface class per §11.2 check (SPF, DMARC, cross-domain report-destination authorization, DKIM selector probing, MX, DNSSEC, MTA-STS, TLS-RPT, BIMI, STARTTLS+cert via a real SMTP handshake, Spamhaus DQS DNSBL/RHSBL, FCrDNS), run by `HealthCheckRunner` against `health_checks`/`health_check_items`. `HealthCheckRunnerFactory` wires the check list (DKIM selectors merge `config('healthcheck.dkim_selectors')` with ones already observed in that domain's reports, so it's rebuilt per domain) — shared by `bin/healthcheck.php` and `DomainController::add()`'s onboarding health check, the two independent call sites that need it. Two DNS primitives: `DnsResolver` (native `dns_get_record()`, used where any resolver is fine) and `DigLookup` (shells out to `dig`, only for DS records and resolver-pinned DNSBL queries — see `config('healthcheck.resolver')`). `error` is always distinct from `fail` (an unconfigured DQS key or a DNS timeout is *unknown*, never reported clean) — this is the one status distinction spec §11.3 insists on. The DMARC check's result also updates `domains.current_published_policy`, same discipline as `ReportStore` (never touches `approved_baseline_policy`). `ReportDestinationAuthCheck` itself derives the destination domain dynamically via live DNS (parsing the domain's own published `rua=` addresses) — `DomainController::crossDomainAuthRecord()` (pure, spec §11.2) instead *predicts* the expected `<policy>._report._dmarc.<destination>` record straight from `config('app.mail_from')`'s domain, so the drill-down can show the exact copy-paste text before that DNS record (or even the domain's own `rua=`) exists yet, not just verify it after the fact. `app.mail_from` was chosen over `imap.username` deliberately — the latter is this deployment's bare IMAP login (`sd-dmarc`), not an email address, so it has no domain part to extract; `mail_from` is guaranteed to be a real address since it's used as an actual `From:` header.

**Auth/RBAC** (`src/Auth/`, `src/Http/`, spec §15): invitation-only accounts, WebAuthn passkeys (preferred) or password+TOTP+recovery-codes (fallback), MFA mandatory for every role. Sessions are a custom server-side store (`SessionManager`, not PHP's native session) so admin actions like "force logout" are a targeted `DELETE`. `AuthMiddleware::guard()`/`guardPost()` enforce deny-by-default RBAC (`read_only ⊂ admin ⊂ super_admin`) and CSRF on every authenticated state-changing route. `StepUp` re-verifies the acting user (current password, or a fresh passkey assertion for passkey-only accounts) before sensitive actions, per spec §15.4/§15.5. `AdminUsersController::resetMfa()` additionally requires a separate `identity_verified` checkbox (not just the free-text `reason`) before clearing a user's MFA — spec §15.5's "consider requiring... out-of-band" re-verification, implemented as a hard requirement rather than a warning label since this codebase already treats spec "should"s as real enforced gates; recorded explicitly (`"identity_verified":true`) in the `user.mfa_reset` audit entry, not just implied by having reached that code path. `AuditLogController` (Super Admin, read-only — spec §15.7) is the viewer for `AuditLog::recent()`, gated above `KnownSendersController`'s Admin tier since the log's contents skew toward the already-Super-Admin-only user-governance actions in `AdminUsersController`. `HibpBreachedPasswordChecker` (spec §15.3, NIST SP 800-63B §5.1.1.2) checks every new/changed password against the HaveIBeenPwned Pwned Passwords corpus via k-anonymity (only a 5-char SHA-1 prefix ever leaves the server) at all three password-setting points — `InviteController::startPassword()`, `SecurityController::changePassword()`, `PasswordResetController::confirm()` — right after each one's existing `PasswordHasher::meetsLengthPolicy()` check. Fails open on any lookup failure (`isBreached()` returns `false`) rather than blocking account creation/password changes on a third-party outage — same "unknown is not a fail" posture `HealthCheckItemResult::ERROR` encodes elsewhere, just with no status to surface here. Uses the same `stream_context_create()`/`file_get_contents()` pattern `MtaStsCheck` already established for outbound HTTP (no Guzzle/cURL anywhere in this codebase).

**Recommendation engine** (`src/Recommendation/`, `bin/analyze.php`, spec §10): one `Rule`-interface class per R1–R12 (`src/Recommendation/Rules/`), each evaluating an `AnalysisContext` (`AnalysisContextBuilder` aggregates `report_records`/`auth_results` into per-IP `SourceStat`s over two windows, resolves known/unknown via `KnownSenderMatcher::matchForDomain()` — domain-scoped, not the plain `match()` enrichment uses — and reuses `HealthCheck\Checks\SpfCheck` for a live SPF DNS-lookup count per spec §10.2). `RecommendationReconciler` is a pure diff (this run's findings vs. previously open/acknowledged rows, matched on `(ruleId, subject)`) that drives insert/touch/auto-resolve against `RecommendationRepository` — idempotent re-runs and auto-resolution (spec §10.5) both fall out of this rather than being tracked separately. Suppressed rows are excluded from reconciliation entirely so "stop showing" sticks. `subject` (the source IP for R1/R2/R3/R12, `null` for the domain-wide rules) is round-tripped through a reserved `_subject` key inside `evidence_json` since the schema has no dedicated column for it.

**Alerting** (`src/Alerting/`, `bin/alert.php`, spec §8): daily cron, independent of `bin/analyze.php`. One `AlertCheck`-interface class per condition (`src/Alerting/Checks/`) evaluating an `AlertContext` (`AlertContextBuilder`, not unit-tested by design like `AnalysisContextBuilder`): `HeartbeatCheck` (no reports in N days — the dead-man's-switch, falls back to `domains.created_at` when a domain has never received a report, so onboarding doesn't false-positive on day one), `PolicyDriftCheck` (same condition as R9, recomputed directly against `domains` rather than depending on `bin/analyze.php` having run, so this script stays standalone-runnable), `UnknownVolumeCheck` (per-IP volume over threshold using the plain, non-domain-scoped `ip_enrichment.label` — deliberately not `KnownSenderMatcher::matchForDomain()`, keeping this a lightweight daily tripwire rather than coupling to the recommendation engine), and `PassRateRegressionCheck` (today vs. a trailing baseline window, skipped below a minimum sample size in either window). All findings across all domains are combined into one digest email via `App\Auth\Mailer` (reused as-is) rather than one per domain/condition. There's no suppression/dedup state — a condition still true on the next run re-alerts, same spirit as the heartbeat's own purpose.

**Dashboard** (`src/Http/Controllers/DomainController.php`, `public/index.php`, spec §7): plain PHP pages via `View` (a page-shell helper, not a template engine — body HTML is built by string concatenation/`sprintf` inside each controller). One controller owns the whole domain surface — list (`index()`, spec §7.1), drill-down (`show()`/`reportDetail()`, spec §7.2), and every mutating action (`add()`, `approveBaseline()`, `updatePolicy()`, `deactivate()`/`reactivate()`) — rather than splitting list vs. drill-down, since `index()` needed the same error/flash re-render machinery a mutating form requires, which the old inline-closure route in `public/index.php` couldn't provide. `Router` (`src/Http/Router.php`) is exact-string-match only, no path params, so the drill-down is addressed by query string (`/domain?domain=example.com`) rather than `/domain/{name}`. Its overview panel's pass/fail trend is a hand-rolled inline `<svg>` (`SvgBarChart`) rather than a vendored Chart.js — no JS dependency exists anywhere in the dashboard today, and the strict `default-src 'self'` CSP would require self-hosting any such library; the SVG's bar colors reference the page's own CSS custom properties via `var()` so it stays theme-correct for free. `RecommendationRepository::forDisplay()`/`HealthCheckRepository::latestForDomain()` are read-only additions alongside those classes' existing write paths, returning full display rows (`RecommendationRow`, `HealthCheckSummary`) rather than the minimal reconciliation-only shapes those classes already had. The report-detail drill-down's IDOR guard is structural, not a separate check: `report_id` lookups always filter `WHERE domain_id = ?` in the same query, so a report belonging to a different domain simply doesn't match rather than requiring an explicit ownership comparison after the fact. `add()` runs the full health-check suite synchronously (`set_time_limit(120)` — a rare, deliberate admin action, not a hot path) so onboarding gets an immediate baseline per spec §11.1, rather than waiting for the next scheduled/manual `bin/healthcheck.php` pass; `approveBaseline()` refuses (never silently seeds) until `current_published_policy` has actually been observed, per spec §10.6, and sets `baseline_approved_at` alongside `approved_baseline_policy`. Domain management splits by tier per spec §15.1's role table: `add()`/`approveBaseline()`/`updatePolicy()` (edits `target_policy`/`non_sending`, scoped to just the `p`/`sp` DMARC tags via `PolicyLevel::compose()`/`isValidLevel()` since no rule reads anything else off `target_policy`) are Admin; `deactivate()`/`reactivate()` are Super-admin and step-up-gated (spec §15.3), reusing `StepUp`/`AuthMiddleware` the same way `AdminUsersController` does. Deactivation is a soft `active=0` flip, not a `DELETE` — every cron script (`ReportStore`, `bin/healthcheck.php`, `bin/analyze.php`, `bin/alert.php`) already filters `WHERE active = 1`, so it drops a domain from every pipeline while report/health-check history stays intact per spec §13. `findDomain()` deliberately has no `active` filter (unlike `renderIndex()`'s separate query, which does) so a deactivated domain's drill-down stays reachable to view history and reactivate it; `approveBaseline()`/`updatePolicy()` both explicitly refuse on an inactive domain as a result.

**Overview dashboard** (`DomainController::renderIndex()`, spec §7.1): a posture-card grid (one per active domain — published policy, `healthGrade()`'s worst-status-wins badge from `HealthCheckRepository::latestForAllDomains()`, a 14-day `SvgBarChart::renderSparkline()`, open-recommendation-by-severity badges from `RecommendationRepository::countsBySeverityForAllDomains()`), an Attention panel (`RecommendationRepository::topOpenAcrossDomains()`, high/medium only), an ingestion-health indicator folded into the existing stats strip (`ingest_runs`, previously write-only), and a recent-activity feed merging `AuditLog::recent()` (previously insert-only; `$actionPrefix` serves both this and the later Audit Log Viewer) with newly-seen-unknown-sender rows from `ip_enrichment.first_seen` (added by migration `0005`, set only on first `INSERT` by `ReportStore::touchEnrichment()`, never touched by its `ON DUPLICATE KEY UPDATE`). Two deliberate deviations from the spec's literal wording, both because the app has no event-sourced/alert-history table anywhere: the Attention panel's "recent High alerts" is sourced from persisted `recommendations`, not a live re-run of the deliberately-stateless `AlertCheck`s (see Alerting below); "policy changes detected" in recent activity means `domain.policy_updated`/`domain.baseline_approved` audit entries (deliberate admin actions), not DNS-observed drift, since the latter is never persisted (`HealthCheckRepository::updatePublishedPolicy()` is a silent passive write).

**Help system** (`src/Help/`, `HelpController`, `docs/feature-helpsystem.md`, beyond the original spec): an 85-article "DMARC 101" catalog (`HelpArticle`/`HelpRepository`, `src/Help/content/*.php` — one file per topic area: dmarc, spf, dkim, healthcheck, rules, alerting, policy, general, pages, sections) written against this codebase's actual behavior, not generic textbook content. `HelpRepository` is pure/PDO-free and throws on a duplicate slug across content files rather than silently shadowing. `index()`/`show()` are public (no tenant data, shareable links); `inline()` (the tooltip JSON fetch) stays auth-gated like the rest of the dashboard. Two deviations from the doc: routes are `/help/article?slug=`/`/help/inline?slug=`, not the doc's literal `/help/<slug>` — `Router` is exact-string-match only (see below), same reason `/domain` uses `?domain=`; and the doc's §5.4 health-check "categories" table (SPF, MX, DNSSEC, ...) actually lists check *names* — the real `category` column only has 3 values (`dns`/`transport`/`reputation`) — `DomainController::healthCheckHelpSlug()` is the checkName→slug mapping. `View::helpTooltip()` renders a "?" trigger (the existing `help-circle` sprite icon, never a literal glyph) that `public/assets/help.js` wires up: fetches `/help/inline?slug=`, then renders the popover as a **floating overlay** (`position: fixed`, positioned via `getBoundingClientRect()`, appended to `<body>`) rather than inserting it in document flow — an in-flow popover was tried first but this dashboard is grid-heavy (`.health-grid`, `.overview-grid`, `.posture-grid`, `.stats`), and CSS Grid stretches every cell in a row to match the tallest one, so any in-flow content growth in one cell inflated the whole row. `tests/Help/HelpCatalogCompletenessTest.php` ties the catalog to the actual `Rule`/`HealthCheckItemResult`/`Alert`-check code so a future R13 or a new status can't silently ship a dead tooltip link. Not yet built (tracked in `docs/todo.md`): a persistent per-page help panel, an onboarding guided walkthrough, and search.

**Landing page** (`LandingController`, `docs/feature-landing-page.md`, beyond the original spec): `/` no longer unconditionally redirects to `/login` — `public/index.php`'s `/` route is a closure (matching the existing `/healthz` closure-route precedent, since `Router` has no path-param/conditional-routing support) that dispatches to `LandingController::show()` (no session) or `DomainController::index($user)` (active session, unchanged). `LandingController` has no dependencies (no PDO, no config) — hero, a 3-paragraph about section, a 6-item capabilities grid, and a link into the help system. Two deviations from the doc: no `HELP_SYSTEM_ENABLED` fallback flag (the help system is live, so the link is unconditional) and no version number/source-link footer initially (this codebase has no version-tracking mechanism anywhere, and per this file's own framing it's a private tool for an internal team) — a GitHub source link was added afterward on request, framed around the fact that invitation-only accounts (spec §15.2) mean there's no signup path, so self-hosting is the only way for someone else to use it.

## Conventions from the spec worth knowing before touching related code

- **Recommendation engine (spec §10)**: rules (R1–R12) are one class each (`src/Recommendation/Rules/`), not hardcoded conditionals in one place, and every recommendation cites the exact evidence (counts, IPs, date range) that triggered it via `RuleFinding::$evidence`. `known` vs `unknown` sender classification (via `matchForDomain()`, domain-scoped) changes a finding's meaning, not just its severity — R1–R3 (known-sender hygiene) vs R5–R6 (unknown-source spoofing) hinge entirely on it.
- **Never recommend inbound blocking from `rua` data alone** (spec §9.4/§10.1) — a domain spoofing *to* third parties isn't necessarily hitting this server; only "investigate," never "block," unless also observed on the local MX.
- **Health checks** (`bin/healthcheck.php`, spec §11) must distinguish `error` (DNS timeout, blocklist query failure) from `fail` (confirmed bad) — an unresolvable check is not a passing check. DNSBL/RHSBL lookups go through Spamhaus's free DQS (keyed), not the public mirrors, which block non-attributable resolvers including most hosting networks (spec §11.6).
- **Auth** (spec §15): three global roles (read-only ⊂ admin ⊂ super admin), invitation-only account creation, MFA mandatory for everyone (passkey preferred, TOTP+password fallback), server-side role enforcement on every endpoint — the router explicitly does not grant access by route registration alone (see the NOTE in `src/Http/Router.php`).
- **Data protection** (spec §14): most `source_ip` values are mail-server IPs operated by organizations, not GDPR personal data, but a residual subset (self-hosters, compromised residential hosts) can be — don't assume either blanket position when touching enrichment/storage/retention code.
